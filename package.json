{
  "name": "sync-timer",
  "version": "1.0.0",
  "private": true,
  "description": "A lightweight real-time shared timer powered by Socket.IO.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "node --watch app.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "socket.io": "^4.8.1",
    "uniqid": "^5.4.0"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.3"
  }
}
